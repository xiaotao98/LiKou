package com.LiKou.simple.liuyuefen2022;

/**
 * 翻转二叉树
 * <p>
 * 思路：先找到二叉树的左子树和右子树，然后将根节点的左右子树进行一个交换
 */
public class title226 {
    /*
    public TreeNode invertTree(TreeNode root) {
        if (root == null) {
            return null;
        }
        TreeNode l = invertTree(root.left);
        TreeNode r = invertTree(root.right);
        root.left = r;
        root.right = l;
        return root;
    }
    */
}
